# Direct dependency selector (hand-edited).
#
# Edit this file to add/remove/change top-level packages, then re-lock with:
#     uv pip compile requirements.in --universal --generate-hashes --python-version 3.12 -o requirements.txt
#
# requirements.txt is the GENERATED, fully-pinned lock (entire dependency tree +
# hashes) and is what gets installed by setup scripts, CI and deploy.
# Do not edit requirements.txt by hand.

# fastapi 0.137+ pulls in starlette 1.3.x (fixes GHSA-jp82-jpqv-5vv3 / GHSA-82w8-qh3p-5jfq).
fastapi==0.137.2
# Starlette CVE fixes (GHSA-jp82-jpqv-5vv3 in 1.3.0, GHSA-82w8-qh3p-5jfq in 1.3.1).
# FastAPI >=0.137 only requires starlette>=0.46.0, so we have to pin directly.
starlette>=1.3.1
uvicorn==0.46.0
aiofiles==25.1.0
fastapi-azure-auth==5.2.0
python-dotenv==1.2.2
pydantic==2.13.3
requests==2.33.1
# Required for health stats
psutil==7.2.2
# Required for logging
opencensus-ext-azure==1.1.15
opencensus-ext-fastapi==0.1.0
# print console
rich==15.0.0
debugpy==1.8.20
#testing
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.28.1
pytest-cov==7.1.0
# websockets
websockets==16.0
# JWT verification. PyJWT replaces the unmaintained python-jose and
# avoids the unpatched ecdsa CVE (PYSEC-2026-1325) by not pulling it in.
# The [crypto] extra pulls in `cryptography` for RS256 verification.
pyjwt[crypto]==2.13.0
# Pin `cryptography` directly: the pyjwt[crypto] extra only requires >=3.4.0
# which is far too loose. >=49.0.0 fixes PYSEC-2026-3553 and PYSEC-2026-3554
# (GHSA-j2h6-j472-rp4c / GHSA-m4p7-r5xf-jp4c).
cryptography>=49.0.0,<51
# pyasn1 is still pulled in transitively via google-auth -> pyasn1-modules;
# pin directly so we get the PYSEC-2026-3455 / 3456 / 3457 fixes.
pyasn1>=0.6.4
# In Memory DB
tinydb==4.8.2
# Cosmos / Azure integration
azure-cosmos==4.15.0
azure-identity==1.25.3
# azure-identity 1.21+ moved VisualStudioCodeCredential behind this package.
# pymsalruntime ships Windows and macOS wheels only, so gate by platform —
# Linux CI/runtime uses ManagedIdentityCredential or AzureCliCredential anyway.
azure-identity-broker==1.3.0; sys_platform == "win32" or sys_platform == "darwin"