

export default class JestPreviewReporter {
  // async onTestResult(test, testResult) {
  //   if (testResult.numFailingTests > 0 && typeof document !== 'undefined') {
  //     const { debug } = await import('jest-preview');
  //     // Automatically show the preview when any test fails
  //     debug();
  //   }
  // }
}